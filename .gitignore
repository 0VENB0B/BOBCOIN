__pycache__/
*.py[cod]
.pytest_cache/
.coverage
htmlcov/

.env
*.env
*firebase-adminsdk*.json
*service-account*.json
*credentials*.json

# local dev tooling (Freebuff desktop DB, venvs)
.freebuff/
venv/
.venv/

*.tmp

# runtime image outputs (defensive — commands send via BytesIO, never saved)
text.png
TID.png
